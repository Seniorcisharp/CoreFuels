
@{
    ViewBag.Title = "Home Page";
}

<div >
    <div class ="main">
        <div class="jersey">
            @if (!User.Identity.IsAuthenticated)
            {
                <a href="@Url.Action("Index", "Authorization")">
                    <img src="/Images/8.png" alt="jersey Image">
                </a>
            }
            else
            {
                <a href="@Url.Action("Index", "Training")">
                    <img src="/Images/8.png" alt="jersey Image">
                </a>
            }
        </div>
        <div class="fruit">
            @if (!User.Identity.IsAuthenticated)
            {
                <a href="@Url.Action("Index", "Authorization")">
                    <img src="/Images/9.png" alt="fruit Image">
                </a>
            }
            else
            {
                <a href="@Url.Action("Index", "Eat")">
                    <img src="/Images/9.png" alt="fruit Image">
                </a>
            }
        </div>

    </div>
    
    
</div>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}